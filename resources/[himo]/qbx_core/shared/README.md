# Runtime vehicle catalogue

The txAdmin recipe installs the pinned upstream Qbox `shared/vehicles.lua` file into this directory as `vehicles.lua` for compatibility with resources that read `QBCore.Shared.Vehicles` or `qbx_core:GetVehiclesByName()`.

HimotheeCore keeps owned-vehicle persistence in `qbx_vehicles`; this catalogue is model metadata only.
