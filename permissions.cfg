# HimotheeCore ACE permissions
# txAdmin's {{addPrincipalsMaster}} lines in server.cfg place the deploying
# admin/master identifiers into group.admin. These Himothee permissions are
# then inherited by that group.
add_ace group.admin himo.admin allow
add_ace group.admin himo.staff allow
add_ace group.admin himo.dev allow
